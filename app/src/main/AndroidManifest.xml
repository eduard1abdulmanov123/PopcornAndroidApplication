<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.abdulmanov.MoviCorn"
    android:installLocation="preferExternal">

    <application
        android:name=".BaseApp"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        android:usesCleartextTraffic="true">
        <activity
            android:name=".ui.library.library_action.LibraryActionActivity"
            android:screenOrientation="portrait"/>
        <activity
            android:name=".ui.filters.FiltersActivity"
            android:screenOrientation="portrait"
            android:label="@string/title_activity_filter" />
        <activity
            android:name=".ui.details_person.DetailsPersonActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.details_movie.DetailsMovieActivity"
            android:screenOrientation="portrait" />
        <activity
            android:name=".ui.MainActivity"
            android:screenOrientation="portrait"
            android:windowSoftInputMode="adjustPan">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

    <uses-permission android:name="android.permission.INTERNET" />

</manifest>